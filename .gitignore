node_modules/
.DS_Store
*.log
.env
.env.*
!.env.example
coverage/
.vitest-cache/
.idea/
.vscode/*
!.vscode/extensions.json
# dist/ is build output, regenerated by `pnpm build`. Never commit the
# exact published bytes — they must reproduce from a tagged commit.
dist
# .deepsec/ is a local security-scan workspace (deepsec tool); managed by
# the tool, not part of this repo's tracked surface.
.deepsec